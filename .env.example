# API 根路径
# - 开发默认用相对路径 /api，由 Vite proxy 转发（见 vite.config.ts）
# - 直连公网联调后端时可设为：https://api.qgents.dpdns.org/api/v1
VITE_API_BASE_URL=/api

# true = MSW mock；false = 打真实后端（proxy 或直连）
VITE_USE_MOCK=false
